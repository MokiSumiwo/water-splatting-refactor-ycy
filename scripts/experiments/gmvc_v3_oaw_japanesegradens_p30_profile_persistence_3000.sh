#!/usr/bin/env bash
set -euo pipefail

REPO_DIR="/mnt/new/home_old/ycy/water-splatting-refactor"

exec env \
  VARIANT="${VARIANT:-P30}" \
  GPU="${GPU:-7}" \
  LOAD_STEP="${LOAD_STEP:-10000}" \
  TARGET_FINAL_STEP="${TARGET_FINAL_STEP:-13000}" \
  MAX_NUM_ITERATIONS="${MAX_NUM_ITERATIONS:-3000}" \
  MODEL_NUM_STEPS="${MODEL_NUM_STEPS:-${TARGET_FINAL_STEP:-13000}}" \
  STEPS_PER_SAVE="${STEPS_PER_SAVE:-1000}" \
  SAVE_ONLY_LATEST_CHECKPOINT="${SAVE_ONLY_LATEST_CHECKPOINT:-False}" \
  EXPERIMENT_NAME="${EXPERIMENT_NAME:-gmvc_v3_oaw_r500_p30_profile_persistence3k_japanesegradens_seed42_step10000_to_13000}" \
  STAMP="${STAMP:-20260806_gmvc_v3_oaw_japanesegradens_r500_profile_persistence_3k}" \
  GMVC_PROFILE_OBSERVABILITY_WEIGHT_ENABLED="${GMVC_PROFILE_OBSERVABILITY_WEIGHT_ENABLED:-True}" \
  GMVC_PROFILE_OBSERVABILITY_WEIGHT_MODE="${GMVC_PROFILE_OBSERVABILITY_WEIGHT_MODE:-dtb_view}" \
  GMVC_PROFILE_OBSERVABILITY_WEIGHT_POWER="${GMVC_PROFILE_OBSERVABILITY_WEIGHT_POWER:-1.0}" \
  GMVC_PROFILE_OBSERVABILITY_WEIGHT_MIN="${GMVC_PROFILE_OBSERVABILITY_WEIGHT_MIN:-0.25}" \
  GMVC_PROFILE_OBSERVABILITY_WEIGHT_MAX="${GMVC_PROFILE_OBSERVABILITY_WEIGHT_MAX:-4.0}" \
  GMVC_GRAD_LOG_EVERY="${GMVC_GRAD_LOG_EVERY:-49}" \
  GMVC_GRAD_LOG_FORCE_STEPS="${GMVC_GRAD_LOG_FORCE_STEPS:-10000,10001,10500,11000,12000,13000}" \
  "${REPO_DIR}/scripts/experiments/gmvc_v3_japanesegradens_r500_profile_persistence_3000.sh"

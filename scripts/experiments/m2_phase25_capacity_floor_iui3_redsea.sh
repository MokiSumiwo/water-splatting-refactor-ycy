#!/usr/bin/env bash
set -euo pipefail

REPO_DIR="/mnt/new/home_old/ycy/water-splatting-refactor"

export GPU="${GPU:-8}"
export DATA_PATH="${DATA_PATH:-${REPO_DIR}/undistorted_data/undistorted_IUI3-RedSea}"
export OUTPUT_DIR="${OUTPUT_DIR:-${REPO_DIR}/outputs}"
export RENDER_ROOT="${RENDER_ROOT:-${REPO_DIR}/renders}"
export LOG_ROOT="${LOG_ROOT:-${REPO_DIR}/logs}"

export MAX_NUM_ITERATIONS="${MAX_NUM_ITERATIONS:-15000}"
export RUN_EVAL="${RUN_EVAL:-1}"
export SEED="${SEED:-42}"
export MEDIUM_CONTEXT_MODE="${MEDIUM_CONTEXT_MODE:-dir_xy_camera}"
export OWNERSHIP_MODE="${OWNERSHIP_MODE:-alpha_depth}"
export INFINITE_WATER_OCCUPANCY_LIMITED="${INFINITE_WATER_OCCUPANCY_LIMITED:-True}"
export INFINITE_WATER_COMPOSE_MODE="${INFINITE_WATER_COMPOSE_MODE:-rgb_mix}"
export INFINITE_WATER_DEPTH_MID="${INFINITE_WATER_DEPTH_MID:-0.75}"
export INFINITE_WATER_DEPTH_TEMP="${INFINITE_WATER_DEPTH_TEMP:-0.15}"
export INFINITE_WATER_CAPACITY_SUPPORT_MODE="${INFINITE_WATER_CAPACITY_SUPPORT_MODE:-m_inf}"
export INFINITE_WATER_HIT_ALPHA_THRESHOLD="${INFINITE_WATER_HIT_ALPHA_THRESHOLD:-0.20}"
export INFINITE_WATER_HIT_ALPHA_TEMP="${INFINITE_WATER_HIT_ALPHA_TEMP:-0.05}"
export INFINITE_WATER_HIT_CONCENTRATION_KAPPA="${INFINITE_WATER_HIT_CONCENTRATION_KAPPA:-0.20}"
export INFINITE_WATER_HIT_PROTECTION_TEMP="${INFINITE_WATER_HIT_PROTECTION_TEMP:-0.05}"
export INFINITE_WATER_HIT_PROTECTION_START_STEP="${INFINITE_WATER_HIT_PROTECTION_START_STEP:-0}"
export BINF_RGB_WEIGHT="${BINF_RGB_WEIGHT:-0.005}"
export ACCUM_ZERO_WEIGHT="${ACCUM_ZERO_WEIGHT:-0.002}"
export NEAR_ZERO_WEIGHT="${NEAR_ZERO_WEIGHT:-0}"
export LOSS_START_STEP="${LOSS_START_STEP:-1000}"
export LOSS_RAMP_STEPS="${LOSS_RAMP_STEPS:-3000}"

CAPACITY_FLOOR_GRID="${CAPACITY_FLOOR_GRID:-C0:False:0.80:1.00 C1:True:0.60:0.50 C2:True:0.80:0.50 C3:True:0.80:0.75 C4:True:0.80:0.25}"
STAMP="${STAMP:-$(date -u +%Y%m%d_%H%M%S)}"
export STAMP

for ITEM in ${CAPACITY_FLOOR_GRID}; do
  TAG="${ITEM%%:*}"
  REST="${ITEM#*:}"
  export INFINITE_WATER_HIT_PROTECTION_ENABLED="${REST%%:*}"
  REST="${REST#*:}"
  export INFINITE_WATER_HIT_PROTECTION_THRESHOLD="${REST%%:*}"
  export INFINITE_WATER_CAPACITY_FLOOR="${REST#*:}"
  export EXPERIMENT_NAME="${EXPERIMENT_NAME_PREFIX:-m2_p25_capacity_floor}_${TAG}_tau${INFINITE_WATER_HIT_PROTECTION_THRESHOLD/./p}_floor${INFINITE_WATER_CAPACITY_FLOOR/./p}_seed${SEED}_${MEDIUM_CONTEXT_MODE}_iui3_redsea_${MAX_NUM_ITERATIONS}"
  unset TIMESTAMP
  bash "${REPO_DIR}/scripts/experiments/m2_infinite_water_iui3_redsea.sh"
done
